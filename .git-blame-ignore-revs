# Commits that only reformat code; `git blame` skips them.
# GitHub's blame view reads this file automatically. Locally, run once:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# Add one full commit hash per line, with a comment saying what it was.

# Format the codebase with Biome (formatting only)
04913212eb7feb75f28108e0c9a3bf63fd9b4c90
